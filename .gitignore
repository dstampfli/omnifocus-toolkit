# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets / config (keep .env.example tracked)
.env

# Local agent working files (task ledgers, scratch issues) — never publish
.superpowers/
.scratch/
